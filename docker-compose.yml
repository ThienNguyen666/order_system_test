services:

  # ── 1. Database ─────────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7.0
    container_name: order-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret
      MONGO_INITDB_DATABASE: order_db
    volumes:
      - mongo_data:/data/db
    networks:
      - order-net
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ── 2. Conductor (local, no auth required) ──────────────────────────────────
  conductor:
    image: orkesio/orkes-conductor-community:latest
    container_name: order-conductor
    restart: unless-stopped
    ports:
      - "8085:8080"   # UI + API → http://localhost:8085
    networks:
      - order-net
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 15
      start_period: 90s

  # ── 3. Spring Boot Backend ───────────────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: order-backend
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SPRING_DATA_MONGODB_URI: mongodb://admin:secret@mongodb:27017/order_db?authSource=admin
      CONDUCTOR_SERVER_URL: http://conductor:8080/api
    depends_on:
      mongodb:
        condition: service_healthy
      conductor:
        condition: service_healthy
    networks:
      - order-net

  # ── 4. React Frontend ────────────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: order-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - order-net

volumes:
  mongo_data:
    driver: local

networks:
  order-net:
    driver: bridge
